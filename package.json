{
  "name": "petstore-api-tests",
  "version": "1.0.0",
  "description": "Swagger PetStore API test otomasyon projesi",
  "main": "index.js",
  "scripts": {
    "test": "newman run PetStore_Collection.json -e PetStore_Environment.json -r cli,allure",
    "report": "allure serve allure-results"
  },
  "devDependencies": {
    "allure-commandline": "^2.24.1",
    "newman": "^6.0.0",
    "newman-reporter-allure": "^3.0.0"
  }
}